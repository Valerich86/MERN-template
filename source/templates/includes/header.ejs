<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/picnic">
    <link rel="stylesheet" href="/styles.css">
    <title><%= title %> :: <%= appTitle%></title>
</head>
<body>
    <nav>
        <% if (req.user) {%>
            <a href="/" class="brand logo">
                <div class="heading"><i>myCase</i></div>
            </a>
        <% } %>
        <input id="bmenub" type="checkbox" class="show">
        <label for="bmenub" class="burger pseudo button">&#9776;</label>
        <div class="menu">
            <% if (req.user) {%>
                <div class="horizontal user"><%= req.user.name %></div>
                <form action="/logout" method="post" class="horizontal">
                    <input type="submit" value="Выйти">
                </form>
            <% }else{ %>
                <a href="/login/" class="button success">Вход</a>
                <a href="/register/" class="button">Регистрация</a>
            <% } %>
        </div>
    </nav>
    